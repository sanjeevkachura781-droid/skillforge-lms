import { ChevronDown, CirclePlay, LockKeyhole } from 'lucide-react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { Link, useParams } from 'react-router-dom';
import { catalogApi, studentApi } from '../api/endpoints';
import { useAuth } from '../context/AuthContext';
import { apiErrorMessage } from '../api/client';
import { ErrorState, LoadingState } from '../components/State';
import { useState } from 'react';

export function CourseDetailsPage() { const { slug = '' } = useParams(); const { user } = useAuth(); const client = useQueryClient(); const [error, setError] = useState(''); const course = useQuery({ queryKey: ['course', slug], queryFn: () => catalogApi.course(slug).then((r) => r.data.data) }); const enroll = useMutation({ mutationFn: () => studentApi.enroll(course.data!.id), onSuccess: () => client.invalidateQueries({ queryKey: ['enrollments'] }), onError: (err) => setError(apiErrorMessage(err)) }); if (course.isLoading) return <LoadingState />; if (course.isError || !course.data) return <ErrorState message="This course is not available." />; const item = course.data; return <section className="mx-auto max-w-7xl px-5 py-12 lg:px-8"><Link to="/courses" className="font-sans text-sm text-[#657066]">← Back to catalog</Link><div className="mt-8 grid gap-12 lg:grid-cols-[1fr_340px]"><div><p className="font-sans text-xs font-bold uppercase tracking-[.25em] text-[#d86445]">{item.category?.name ?? 'Course'}</p><h1 className="mt-3 max-w-3xl text-6xl leading-none">{item.title}</h1><p className="mt-6 max-w-2xl font-sans text-lg leading-8 text-[#657066]">{item.description}</p><div className="mt-8 border-t border-[#d8d2c3] pt-7"><h2 className="text-3xl">Course outline</h2><div className="mt-5 space-y-3">{item.modules?.map((module) => <details key={module.id} className="group border border-[#d8d2c3] bg-[#fbfaf5]" open><summary className="flex cursor-pointer list-none items-center justify-between p-5 font-sans font-bold">{module.title}<ChevronDown className="transition group-open:rotate-180" size={18} /></summary><div className="border-t border-[#e6e1d6] px-5 pb-4">{module.lessons?.map((lesson) => <div key={lesson.id} className="flex items-center gap-3 border-b border-[#eee9df] py-4 font-sans text-sm last:border-0"><CirclePlay size={16} className="text-[#d86445]" /><span className="flex-1">{lesson.title}</span>{lesson.isPreview ? <span className="text-xs text-[#d86445]">Preview</span> : <LockKeyhole size={14} className="text-[#8c958c]" />}</div>)}</div></details>)}</div></div></div><aside className="h-fit border border-[#d8d2c3] bg-[#193b31] p-7 text-white"><div className="text-5xl text-[#edbd61]">{item.level.slice(0, 1).toUpperCase()}</div><p className="mt-4 font-sans text-xs uppercase tracking-[.2em] text-[#b8c7bc]">{item.level} level</p><p className="mt-3 font-sans text-sm leading-6 text-[#dbe3db]">{item.shortDescription}</p>{user?.role === 'student' ? <button disabled={enroll.isPending} onClick={() => enroll.mutate()} className="mt-7 w-full rounded-full bg-[#d86445] px-5 py-3 font-sans text-sm font-bold text-white disabled:opacity-60">{enroll.isPending ? 'Enrolling...' : 'Enroll in course'}</button> : !user ? <Link to="/login" className="mt-7 block rounded-full bg-[#edbd61] px-5 py-3 text-center font-sans text-sm font-bold text-[#18221d]">Sign in to enroll</Link> : null}{error && <p className="mt-3 font-sans text-xs text-[#ffc9ba]">{error}</p>}</aside></div></section>; }
